{
  "name": "ts-import-text",
  "version": "0.1.1",
  "description": "Allow importing text files in TS.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "ttsc",
    "test": "yarn build && mocha 'dist/test/**/*.test.js'",
    "prepublishOnly": "tsc"
  },
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "typescript",
    "transform",
    "text"
  ],
  "devDependencies": {
    "@types/chai": "^4.2.3",
    "@types/mocha": "^8.0.0",
    "@types/node": "^14.0.18",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "ts-node": "^8.4.1",
    "ttypescript": "^1.5.12"
  },
  "dependencies": {
    "typescript": "^3.6.3"
  }
}
